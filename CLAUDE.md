# Claude Instructions

## Agent Transparency
- For any non-trivial code change, follow `.claude/skills/agent-transparency/SKILL.md`.
- Maintain `agent-activity.html` at the repo root during the task by copying `.claude/skills/agent-transparency/template.html`.
- Edit only the JSON data block in `agent-activity.html`; do not edit the renderer or CSS.
- Keep `agent-activity.html` local unless the human explicitly asks to commit it.
